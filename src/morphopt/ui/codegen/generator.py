from morphopt.ui.model.problem import ProblemModel


class CodeGenerator:
    def generate(self, problem: ProblemModel) -> str:
        # TODO: Generate a standalone V4 task module.
        return ""
